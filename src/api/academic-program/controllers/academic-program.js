'use strict';

/**
 * academic-program controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::academic-program.academic-program');
